<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="model"
            type="com.buttercat.fridgebook.viewmodel.FridgeListViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.FridgeListFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/fridgeList"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/layout_margin_all_8dp"
            android:layout_marginEnd="@dimen/layout_margin_all_8dp"
            android:layout_marginLeft="@dimen/layout_margin_all_8dp"
            android:layout_marginRight="@dimen/layout_margin_all_8dp"
            android:layout_marginStart="@dimen/layout_margin_all_8dp"
            android:layout_marginTop="@dimen/layout_margin_all_8dp"
            android:padding="@dimen/layout_padding_all_4dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:setAdapter="@{model.getAdapter()}"/>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/addItemButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add_black_24dp"
            android:layout_margin="@dimen/layout_margin_all_16dp"
            android:clickable="true"
            android:focusable="true"
            android:onClick="addButtonClicked"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>